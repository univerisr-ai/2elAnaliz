{
  "name": "2el-analiz",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram document analyzer for second-hand GPU listings",
  "type": "module",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "next dev",
    "preview": "next dev",
    "build:next": "next build",
    "build": "npm --prefix apps/api install && npm --prefix apps/api run build && npm --prefix apps/web install && npm --prefix apps/web run build && node -e \"const fs=require('fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.cpSync('apps/web/dist','dist',{recursive:true});\" && node scripts/prepare-static-routes.mjs",
    "analyze:file": "node src/index.mjs --file",
    "dashboard:build": "node src/local-index.mjs ./data/inbox/output.json && node scripts/prepare-api-dashboard-cache.mjs ./data/inbox/output.json",
    "dashboard:api-cache": "node scripts/prepare-api-dashboard-cache.mjs ./data/inbox/output.json",
    "vercel:dev": "npx vercel dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "adm-zip": "^0.5.17",
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "form-data": "^4.0.1",
    "helmet": "^8.1.0",
    "lucide-react": "^1.16.0",
    "multer": "^2.1.1",
    "next": "^16.2.6",
    "node-cron": "^3.0.3",
    "react": "^19.2.6"
  }
}
